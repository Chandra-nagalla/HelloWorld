pipeline {
   echo 'CICD.....'
   stages {

            steps {
                echo "Branch Name:${branchName}"
                echo "${env.JOB_NAME}"
                echo "Build number:${env.BUILD_NUMBER}"
                echo "Branch Name(env): ${env.BRANCH_NAME}"
            }

   }
}